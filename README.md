# Item API

Simple Java Spring Boot REST API using in-memory storage.

## How to run
mvn spring-boot:run

## APIs

POST /api/items  
GET /api/items/{id}

## Notes
 I Uses ArrayList as in-memory storage
 No database
 Input validation enabled
